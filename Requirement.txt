Flask==1.0.2
Flask-API==1.1
Flask-HTTPAuth==3.2.4
httplib2==0.9.2
passlib==1.7.1
tavern==0.22.1

-- While the Python standard library includes a module for making HTTP requests:
          pip3 install --user requests

-- We need library in a single file rfeed.py, so we could simply copy it wherever you need it. we can also install it using the following command:
          python setup.py install
